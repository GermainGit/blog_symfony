<header>
    <div class="logo-site">
        <a href="/"> Mon Blog</a>
    </div>
    <div class="control-section">
        {% if app.user %}
            <button class="btn deconnexion"><a href="{{ path("newPost") }}">Ecrire un article</a><span></span></button>
            <button class="btn deconnexion"><a href="{{ path("app_logout") }}">Deconnexion</a><span></span></button>
        {% else %}
            <button class="btn connection"><a href="{{ path("app_login") }}">Connexion</a><span></span></button>
            <button class="btn inscription"><a href="{{ path("app_register") }}">Inscription</a><span></span></button>
        {% endif %}
    </div>
</header>
